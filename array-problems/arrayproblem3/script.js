/*
Define a function that takes in an array and returns the sorted version of that array.

*/

let initialArray = ["sun", "moon", "stars", "sky"];

function sortedArray(arry) {
	 let newArray = arry.sort();

    return newArray;

}
console.log(sortedArray(initialArray));
